데이터 모델 
DB의 구조를 기술하는데 (설명하는데) 사용될 수 있는 개념들이 모인 집합 (건축구조물 설계도와 같은 개념)
DB구조(데이터 유형, 관계(relationship), 제약사항 등등)를 추상화해서 표현할 수 있는 수단제공 
데이터 모델은 여러 종류가 있으며 추상화 수준과 DB 구조화 방식이 조금씩 다르디
DB에서 읽고 쓰기 위한 기본적 동작들 포함 

data model 분류

[1]개념적 데이터 모델
일반 사용자들이 쉽게 이해할 수 있는 개념들 
데이터베이스 모델을 추상화 수준으로 그려 일반사용자들이 가장 쉽게 이해할 수 있게 만든 모델
비즈니스 요구사항을 추상화하여 기술할 때 사용
대표적 모델 (er 다이어그램 – entity-relationship모델)-엔티티와 관계 + 속성(attribute)

[2]논리적 데이터 모델
이해하기 어렵지 않으면서도 디테일하게 DB를 구조화할 수 있는 개념들을 제공
데이터가 컴퓨터에 저장될 때의 구조와 크게 다르지 않게 DB 구조화를 가능하게 함
특정 DBMS나 storage에 종속되지 않는 수준에서 DB를 구조화할 수 있는 모델
논리데이터모델 종류 
1. relational data model
  데이터를 테이블 형태 저장 =>이 테이블을 relation 이라 부른다. 행은 데이터들 열은 속성들 ex) 오라클 mysql 등 

  기본 키 (Primary Key) #
Data-Table에 있는 유일하게 구분되는 Data-Key를 기본 키(Primary Key)라 부른다.
Primary Key는 유일한 값이기 때문에 중복된 값을 가질 수 없다.
Primary Key는 공백을 가질 수 없습니다.
특징 #
고유성 (Uniqueness)

Primary Key는 각 행(row)을 식별하는 데 사용되며, 모든 값은 고유해야 한다. 즉, 특정 열의 Primary Key 값은 중복되지 않는다.

NULL 값의 허용 불가능 (Non-nullable)

Primary Key 값은 NULL이 될 수 없다. 각 행은 반드시 Primary Key 값을 가져야 한다.

일반적으로 숫자나 문자열

데이터베이스 대부분에서 Primary Key는 정수 또는 문자열과 같은 데이터 타입일 수 있다.

테이블의 기본 정렬 기준

데이터베이스 시스템은 주로 테이블을 Primary Key로 정렬하므로, 검색 및 연산이 효율적으로 수행된다.

기본 키 제약 조건

Primary Key로 지정된 열은 해당 테이블에서 기본 키 제약 조건을 가진다.


외래 키 (Foreign Key) #
한 Table과 참조되는 다른 Table 간의 연결되는 Primary Key Column을 Foreign Key라 합니다.
Foreign Key는 다른 Primary Key를 참조하는 속성 또는 속성들의 집합을 의미한다.
Foreign Key는 참조관계의 기본 키와 같은 속성을 가진다.
특징 #
다른 테이블의 Primary Key를 참조

Foreign Key는 다른 테이블의 Primary Key를 참조하여 관계를 설정한다.

무결성 제약 조건

Foreign Key는 참조된 Primary Key 값이나 NULL이어야 한다. 즉, 참조 무결성을 유지한다.

한 테이블 내에서 외래 키값은 중복 가능

특정 테이블 내에서 Foreign Key 값은 중복될 수 있다.

삭제 및 갱신 규칙 설정 가능

Foreign Key를 사용하여 부모 테이블에서 행을 삭제하거나 갱신할 때 자식 테이블의 관련 행도 자동으로 처리할 수 있는 규칙을 정의할 수 있다.


2. object data model 
  객체개념을 사용해서 데이터베이스를 모델링
3.두개의 종류를 합친 object-relation data model ex) postgreSQL
등등등 있다.

[3]물리적 데이터 모델
컴퓨터에 데이터가 어떻게 파일형태로 저장되는지를 기술할 수 있는 수단을 제공
데이터 포맷(data format)이나 데이터 순서(data orderlings), 데이터 검색을 빠르게 하기 위한 구조체로 불리며 인덱스와 같은 접근 구조체(access path) 등등

스키마
테이블 구성 방식이나 양식이 들어있음


-------------------------------------------------------

# edu 폴더 안의 departments에서 테이블 안에 이름은 물리설계 표현 , 코멘트는 논리설계 표현이라 생각하면 된다. 1:1로 구현된다. 1대다나 다대1로 구성되지 않는다. 그리고 보통 스네이크형식으로 많이 표현된다. 카멜형식도 구현은 가능하다.
# 물리형식 - 길게 표현하지 않고 짧게 쓴다. 실제 sql에서 사용하는 culomm 명 이름 부분! ex) no
# 논리형식 - 사람이 보았을때 식별 할 수 있게 절대로 짧게 쓰지 않고 길게 서술하듯 표현한다.

# 스키마는 데이터가 들어있는 정보 metadata등이 들어있다. 도메인이라고 불리기도 한다. 큰 의미는 스키마고 그 안에 기능중 하나가 도메인이라 하지만 도메인이라고 부르는 사람도 있다. 서비스 데이터들을 크게 모으는 대상 = 도메인

# 뷰와 테이블의 이동 속도는 테이블이 빠르지만 이를 정리해서 올리는 시간이 조인이라는 개념으로 인해 뷰보다 느리기 때문에 뷰로 저장하게 되면 캐시형식으로 테이블화 된다. 오라클에서는 머터리얼뷰라고 해서 뷰를 캐시말고 미리 테이블형식으로 저장해서 관리 할 수 있는 형식이 생겼다. 뷰는 읽기 권한에만 관련 되어있다. 뷰내부에 쪼개서 관리도 가능하다.

# 프로시저 또한 기술이다(PL) vs 함수. 프로시져는 여러가지 기능을 가진 회사 맞춤형 논리 정리 => 오류+ 리턴값 + 반복문 + 가정문 등 비즈니스 로직을 넣을 수 있다. 

# 함수 중 가장 많이 쓰는 것은 1.나라별 타임존을 쓴다. 우리나라 시간 => 해외 시간 2. 달력을 사용한다. 내장함수에 날짜란 개념은 있으나 달력의 개념이 없기 때문에.

# 데이터가 조작될때 실행되는 것을 트리거라 한다. 데이터를 바꿨을 때 연관된 테이블 값을 자동 업데이트 및 제거 가능하게 하는 것

# edu폴더를 관리하는 것이 스키마. 적어도 select문은 기억해라! 그거 모르면 취업은 꿈꾸지도 말아라

# commit 작업이 끝나고 저장해서 다른 영역으로 시작할수 있게 마감하는 역할=> 실제 테이블에 저장됨, rollback 저장했던 것이 문제가 되었을때 다시 돌아가게 하는 것

# 모든 것이 접속시작에서 부터 커밋이나 롤백했을때까지 트랜잭션으로 이동해서 막힌다.

# ddl(최초 생성시) dml(기능 부여시) 플젝시 제일 많이 진행

# 콜은 프로시져 관련 사항 (dml 내에 위치)

# 1대 다 표시 확인 후 1이 모여있는 곳이 부모쪽이기때문에 부모쪽으로 자식들의 정보를 붙인다 => join이다

# 부모와 자식들과의 관계가 연결됬는데 두개 이상 일대 다가 보인다. 이건 매핑 테이블이다.