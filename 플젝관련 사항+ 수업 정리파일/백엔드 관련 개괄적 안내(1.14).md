# 주제 계획표 어떤 기능있는지 나열  만들다보면 생기는 주제 나열 + 추가 +업그레이드 아키텍쳐 (어떻게 웹만들고 프론트 백엔드 데이터관리사항 정리)

# 한국소프트웨어기술자 경력관리시스템? 가입해야한다

# relational -> 웹개발용 , multi-model -> 노sql데이터(비정형데이터)

# 포스트그레sql 벡터데이터베이스 관리 가능

# 관계형 데이터베이스-마리아db (풀스택)=> 벡터데이터베이스- 포스트그래(AI에이젼트) 요새는 sqlite씀(안드로이드 기본 데이터 저장)  빅데이터(아파치 하이브 쓴다) + neo4j (그래픽쪽 데이터)

# 빅데이터 관련 사용이고 이를 어떻게 받아들이고 시작할지 공부

# 앞으로 파이썬 + SQL 기본 공부 완벽히 하기 (프로젝트를 위해)

# html + css + javascript + python + sql ////// 스파크 + 카프카 => 파이널할거 

# 1. 접속 방법 cmd에서 docker images로 확인 -> 2.없다면 docker pull mariadb:12.1.2로 설치 -> 3. docker run -d -p 3306:3306 --env MARIADB_ROOT_PASSWORD=1234 --name mariadb  mariadb:12.1.2 로 포트와 아디 비번생성시켜 도커에서 설치 되어 있는지 확인 -> 4. HeidiSQL로 들어가서 아디 비번 포트 확인후 들어감 => 5. 유저에서 %는 모든 사람 다 들갈 수 있따는 의미 확인

# 호스트 사용자 암호 포트 이용해서 외부에서 데이터를 가져 올 수 있는데  포트는 마리아db에서 왔다.

# 데이터베이스 이동은 쿼리에 use 파일명 쓰면 된다. select * from 파일명.user; => (등록명령어) create user '명'@'공유항목' indentified by '명';  => create database 폴더명; => grant all on 폴더명.* to '명'@'공유항목'

# 자바스크립트와 파이썬은 인덱싱은 둘다 가지고 있다. 하지만 파이썬에만 문자열 슬라이싱이 있다. 

# 인덱싱에서 문자열에서 -찍으면 둘다 작동은 되지만 파이썬만 값이 출력되고 자바스크립트는 값 출력이 안된다.

# 바인딩의 의미가 매칭시킨다. 파이썬은 리스트 튜플 딕셔너리도 중요하지만 문자열 편집 관련한 내용들을 많이 알고 사용할 줄 알아야한다(특히 인덱싱과 슬라이싱). 그리고 문자열 내부함수도 많이 써보고 이용할 줄 알아야한다.
